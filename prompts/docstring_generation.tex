\subsection*{Docstring Generation}

Docstrings were partly generated using AI assistance.

\textbf{Prompt:}
\begin{lstlisting}[language={}, breaklines=true]
Generate a docstring for the following functions and classes. The docstring should be in the already used style format and should include a brief description of the function/class, its parameters, and its return value. The docstring should be concise and informative, providing enough information for a developer to understand the purpose and usage of the function/class without having to read the implementation details.
If needed, apply professional comments to clarify substeps in the function/class implementation.
\end{lstlisting}

\textbf{Context:}
\begin{lstlisting}[language={}, breaklines=true]
Current file (cpp/h), optionally README.md text snippets, and any other relevant information can be used to generate the docstring.
\end{lstlisting}

\textbf{Answer:}
\begin{lstlisting}[language={}, breaklines=true]
Generated docstrings.
\end{lstlisting}
