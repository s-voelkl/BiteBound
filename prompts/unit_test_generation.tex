\subsection*{Unit Test Generation}

To ensure code reliability and maintainability, unit tests were developed for the core hardware components.
The unit test writing was partly assisted by AI.


\textbf{Prompt:}
\begin{lstlisting}[language={}, breaklines=true]
Generate comprehensive unit tests for the following functions and classes. The tests should cover normal cases, edge cases, and error conditions. Each test should be independent, focused on a single behavior, and include clear assertions. Use descriptive test names that indicate what is being tested. Include setup and teardown methods where necessary to ensure test isolation and reproducibility.
\end{lstlisting}

\textbf{Context:}
\begin{lstlisting}[language={}, breaklines=true]
Current file (cpp/h), optionally README.md text snippets, and any other relevant information can be used to generate the unit tests.
\end{lstlisting}

\textbf{Answer:}
\begin{lstlisting}[language={}, breaklines=true]
Generated unit tests.
\end{lstlisting}
